import CartContainer from '../../Cart/CartContainer';

const CartPage = () => {
    return (
        // here could be other containers, components
        <CartContainer />
        // here could be other containers, components
    )
}

export default CartPage;
